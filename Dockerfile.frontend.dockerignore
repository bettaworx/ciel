.git
**/.DS_Store
**/*.log
**/tmp
**/node_modules
**/dist
**/out
**/*.exe
**/*.test
**/*.prof
**/.vscode
**/.turbo
**/coverage

# The frontend image installs from the repository root workspace.
apps/frontend/pnpm-lock.yaml
apps/frontend/pnpm-workspace.yaml

# Secrets and environment files
.env
.env.*
!.env.example

# Not needed for frontend build
apps/backend
# The runtime image is nginx, so nginx/frontend/ is part of the build. The host
# reverse-proxy config under nginx/config/ is not.
nginx/config
scripts
README.md
AGENTS.md
Jenkinsfile
docker-compose*.yml*
run.sh

# Secret key/cert files
**/*.pem
**/*.key
**/*.p12
**/*.pfx
**/*.crt
**/*.cer
