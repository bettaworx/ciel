# Backend API base URL
# - Development: http://localhost:6137
# - Production: https://yourdomain.com (same origin via nginx reverse proxy)
# This URL is used for API requests, WebSocket connections
NEXT_PUBLIC_API_BASE_URL=http://localhost:6137

# Content Security Policy (CSP) configuration
# Controls whether to enforce HTTPS-only resources in production
# - true: Enables upgrade-insecure-requests (forces HTTP to HTTPS, blocks HTTP images/media)
# - false: Allows HTTP resources (useful when behind HTTP reverse proxy or in local testing)
# Default: true in production, false in development
NEXT_PUBLIC_CSP_UPGRADE_INSECURE_REQUESTS=true
