import 'package:test/test.dart';
import 'package:ciel_api/ciel_api.dart';

// tests for MediaServerIconLimitsGif
void main() {
  final instance = MediaServerIconLimitsGifBuilder();
  // TODO add properties to the builder and call build()

  group(MediaServerIconLimitsGif, () {
    // Maximum longest edge size in pixels for animated GIF server icons (aspect ratio preserved). Both animated and static (first frame) versions are created.
    // int maxSize
    test('to test the property `maxSize`', () async {
      // TODO
    });
  });
}
