# Docker Compose Environment Variables
# This file contains environment variables for Docker Compose services (PostgreSQL and Redis)
# Copy this file to .env and set strong passwords

# PostgreSQL Configuration
POSTGRES_DB=ciel
POSTGRES_USER=ciel
# SECURITY CRITICAL: Set strong password (32+ characters recommended)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=

# Redis Configuration
# SECURITY: Set strong password (32+ characters recommended)
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=

# NOTE: These same passwords must also be configured in apps/backend/.env.local
# - POSTGRES_PASSWORD must match the password in DATABASE_URL
# - REDIS_PASSWORD must match the password in REDIS_ADDR or REDIS_PASSWORD
